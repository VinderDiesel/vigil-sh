# 安全政策

## 支持版本

| 版本 | 支持 |
|---|---|
| 0.1.x (v1alpha1) | ✅ |

## 报告漏洞

**请勿开公开 issue。** 请通过 GitHub 的 "Report a vulnerability"（私有安全通告）提交，或私信联系维护者。

请包含：
- 漏洞类型（PII/secret 泄漏、权限绕过、门禁可被绕过、依赖风险…）
- 复现步骤与最小样例（**不要粘贴真实敏感数据**，请用假数据）
- 影响范围与你认为的严重度

我们承诺：确认收到 72 小时内；修复或缓解方案 30 天内；修复发布前不公开细节。

## 本项目特别关注的安全边界

1. **最小化是否真的生效**：`minimizer_default` 的字段白名单与 PII 正则是否漏掉了某类数据。
2. **secret 是否可能落入用例 / JUnit / PR 评论 / 日志**。
3. **硬门禁是否可被绕过**：是否存在任何开关、环境变量或配置能让安全阈值被豁免。
4. **环境是否自我提权**：用例 YAML 是否可能让环境插件执行超出其能力的操作（HARD-8）。
5. **依赖风险**：核心零第三方依赖（HARD-1）；任何新增依赖都需说明其许可与维护状态。

## 冻结规则

一经确认存在 PII / secret 泄漏，立即冻结 `collect` 与导出能力，只保留本地最小化模式，直至复盘完成。
